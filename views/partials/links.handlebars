<div class="container">
    <h6>Some posts about us</h6>
    {{#each data}}
        <div>
            <a href='{{this.url}}' > {{{this.text}}}</a>
        </div>
    {{/each}}
</div>

<style>
    .container{

    }
</style>