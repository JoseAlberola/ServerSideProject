<h2> All about {{person.name}}</h2>

<h3>Name {{person.name}}</h3>

{{#if person.dob}}
<h3>Date of Birth {{person.dob}}</h3>
{{/if}}

<img src={{person.imageurl}} width="150" height="180" alt="photo of the person">

<br></br>

{{#if person.hobbies}}
    <h3>Hobbies</h3>
    {{#each person.hobbies}}
        <span style="list-style-type: none;">{{this}}</span>
        <br>
    {{/each}} 
{{/if}}